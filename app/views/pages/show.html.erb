<div class="container is-fluid">
  <div class="notification">
    <h1><strong><%= @page.title %></strong></h1>
    <br>
    <small>by <%= link_to @page.user.name, users_path(@page.user) %>
      <p>
        <span class="tag is-dark"><%= @page.tag.name %></span></p>
    </small><br>
    <div class="container is-fluid">
      <p><%= @page.content %></p>
    </div>
  </div>
  <% if @page.user_id == current_user.id %>
    <%= link_to 'Edit this page', edit_page_path(@page) %>
  <% else %>
    <%= form_tag likes_path do%>
      <%= hidden_field_tag(:"like[user_id]", current_user.id) %>
      <%= hidden_field_tag(:"like[page_id]", @page.id) %>
      <% if !(@page.likes.any? { |like| like.user_id == current_user.id }) %>
        <%= submit_tag("Like") %>
      <%end%>
    <%end%>
  <%end%>
</div>
