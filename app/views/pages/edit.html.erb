<h1 class="title is-1">Now in EDIT MODE...</h1>
<div class="box">
  <div class="navbar-end">
    <%= link_to(page_path(@page),
      method: :delete,
      :data => {:confirm => 'Are you sure?'},
      class: 'button is-danger') do %>
      <%= content_tag('i', nil, class: 'fas fa-trash') %>
   <% end %>
  </div>

  <%= render 'form' %>
</div>
