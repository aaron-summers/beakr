<% @tags.each do |tag| %>
  <div class="container is-fluid">
    <div class="notification">
    <h3 class="title is-3"><%= link_to tag.name, tag %></h3>
      <% tag.pages.each do |page| %>
        <%= render 'shared/page_preview', page: page%>
      <% end %>
    </div>
  </div><br>
<% end %>
